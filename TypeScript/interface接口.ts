// 包含尺寸和位置的結構體
export interface YQRect {
    x: number;
    y: number;
    width: number;
    height: number;
}

// 包含顏色數值的結構體
export interface YQColor {
    red: number;
    green: number;
    blue: number;
    alpha: number;
}