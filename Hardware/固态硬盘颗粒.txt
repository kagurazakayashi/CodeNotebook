SLC 用两种电压变化来表示 0 或 1（1bit），
MLC 用 4 种电压变化来表示11、10、01、00（2bit），
TLC 用八种电压变化（3bit），
QLC 用 16 种（4bit）。

LC 的平均可擦写100000个周期。
MLC 只有 35000-10000 个周期。
TLC 大约是5000。
QLC 。

TLC 的寿命应该在 5 年以上，但 QLC 只有 3-5 年。

M.2 只是 SSD 的外形，NVMe 则和 SATA 一样是指接口形式。M.2 的固态也有 SATA 接口的。

https://zhuanlan.zhihu.com/p/258503155