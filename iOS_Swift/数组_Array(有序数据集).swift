// Array(有序数据集)

// 空数组
var num = [Int]()

// 默认值数组
var = Array(repeating:0.0 , count: 3)
// 创建一个一维数组 [0.0,0.0,0.0]

// 字面量创建数组
var bre : [String] = ["Eggs","milk"]

// 数组属性：
// count，append，insert，[index]
